---
phase: 10-zenburn-color-scheme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/UI/Color.elm
  - src/index.html
autonomous: true
requirements:
  - COL-01
  - COL-02
  - COL-03
  - COL-04

must_haves:
  truths:
    - "Every page has a warm dark background (~#3f3f3f) instead of near-black (#0d0d0d–#161616)"
    - "Body text renders in Zenburn cream (#dcdccc) instead of cool light grey (#e7e7e7)"
    - "Active/highlighted states (nav, scores, bracket selections) show amber (#f0dfaf) instead of orange (#f77f21)"
    - "The app builds without errors after color changes"
    - "HTML body background and PWA theme-color match the new palette"
  artifacts:
    - path: "src/UI/Color.elm"
      provides: "Single source of truth for all app colors"
      contains: "rgb255 0x3F 0x3F 0x3F"
    - path: "src/index.html"
      provides: "HTML shell with body background and PWA theme-color"
      contains: "#3f3f3f"
  key_links:
    - from: "src/UI/Color.elm"
      to: "src/UI/Style.elm"
      via: "named color imports"
      pattern: "UI.Color as Color"
    - from: "src/UI/Color.elm"
      to: "src/View.elm"
      via: "Color.black used for page backgrounds"
      pattern: "Background.color Color.black"
---

<objective>
Replace the current near-black terminal palette with a warm Zenburn-inspired palette app-wide.

Purpose: The app's visual identity shifts from cold dark (#0d0d0d) to the warm Zenburn aesthetic (#3f3f3f background, #dcdccc text, #f0dfaf amber accents). All color is centralized in UI/Color.elm, so changing named constants propagates everywhere automatically.

Output: Updated UI/Color.elm with Zenburn values, updated index.html with matching body background and PWA theme-color.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current color values being replaced. All other Elm files reference these named exports — no other files need changes. -->

From src/UI/Color.elm (current values → new Zenburn values):

```
black       rgb255 13 13 13       → rgb255 0x3F 0x3F 0x3F   -- #3f3f3f warm dark background
primary     rgb255 18 18 18       → rgb255 0x2A 0x2A 0x2A   -- #2a2a2a slightly darker than bg
primaryDark rgb255 35 35 35       → rgb255 0x35 0x35 0x35   -- #353535 mid-dark panel
primaryLight rgb255 60 60 60      → rgb255 0x55 0x55 0x55   -- #555555 hover/light surface
panel       rgb255 22 22 22       → rgb255 0x3A 0x3A 0x3A   -- #3a3a3a card/panel bg
terminalBorder rgb255 60 60 60    → rgb255 0x4F 0x4F 0x4F   -- #4f4f4f border (slightly lighter than bg)
white       rgb255 0xE7 0xE7 0xE7 → rgb255 0xDC 0xDC 0xCC   -- #dcdccc Zenburn muted cream text
orange      rgb255 0xF7 0x7F 0x21 → rgb255 0xF0 0xDF 0xAF   -- #f0dfaf Zenburn amber accent
```

Colors that do NOT change (semantic, not palette-related):
- red, dark_red, dark_blue, light_blue, grey, shadow
- green, right, wrong, selected, potential
- secondary, secondaryDark (not widely used)

Colors that alias changed ones (auto-updated by changing the base):
- primaryText = white          → becomes #dcdccc automatically
- secondaryText = orange       → becomes #f0dfaf automatically
- secondaryLight = orange      → becomes #f0dfaf automatically
- terminalAccentDim            → update to rgb255 0xCC 0x99 0x66 (dim amber, proportional shift)

From src/index.html (current → new):
- body style="background-color: #0d0d0d"  → background-color: #3f3f3f
- meta name="theme-color" content="#ff8c00" → content="#f0dfaf"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update UI/Color.elm with Zenburn palette</name>
  <files>src/UI/Color.elm</files>
  <action>
Update the named color constants in UI/Color.elm to the Zenburn-inspired values. Do NOT change the function signatures, module declaration, or exports — only change the rgb255 values of the listed colors.

Changes to make:

1. `black` — change from `rgb255 13 13 13` to `rgb255 0x3F 0x3F 0x3F`
   Used as: page/body backgrounds, scoreInput bg, terminalInput bg, scoreButtonSBPotential bg, error bg

2. `primary` — change from `rgb255 18 18 18` to `rgb255 0x2A 0x2A 0x2A`
   Used as: buttonActive fg (text on accent), buttonInactive bg

3. `primaryDark` — change from `rgb255 35 35 35` to `rgb255 0x35 0x35 0x35`
   Used as: darkBox background

4. `primaryLight` — change from `rgb255 60 60 60` to `rgb255 0x55 0x55 0x55`
   Used as: hover background in buttonPotential

5. `panel` — change from `rgb255 22 22 22` to `rgb255 0x3A 0x3A 0x3A`
   Used as: card backgrounds, matchRow, teamBadge, teamButton, buttonPotential, buttonPerhaps

6. `terminalBorder` — change from `rgb255 60 60 60` to `rgb255 0x4F 0x4F 0x4F`
   Used as: borders throughout (must be clearly visible against new panel #3a3a3a)

7. `white` — change from `rgb255 0xE7 0xE7 0xE7` to `rgb255 0xDC 0xDC 0xCC`
   Used as: primaryText (all body text), scoreButtonSBPotential font color

8. `orange` — change from `rgb255 0xF7 0x7F 0x21` to `rgb255 0xF0 0xDF 0xAF`
   Used as: all active/highlight states (header colors, kopje, emphasis, buttonSelected border/text, scoreInput border/text, active matchRow border, nav active, scoreButtonSBPotential hover, buttonPill hover, buttonPillA, buttonPillB, link color, secondaryText alias, secondaryLight alias)

9. `terminalAccentDim` — change from `rgb255 180 90 15` to `rgb255 0xCC 0x99 0x66`
   Proportional shift: dim amber to match new amber palette

Do NOT change: red, dark_red, dark_blue, light_blue, grey, shadow, green, right, wrong, selected, potential, secondary, secondaryDark.

The aliases (primaryText = white, secondaryText = orange, secondaryLight = orange, right = green, wrong = red, selected = black, potential = white) do not need touching — they will automatically reflect the updated base values.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make build 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Build succeeds (exit 0). All 9 color constants updated to Zenburn values. No other files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html body background and PWA theme-color</name>
  <files>src/index.html</files>
  <action>
Update two values in src/index.html to match the new Zenburn palette:

1. Body inline style — change:
   `background-color: #0d0d0d`
   to:
   `background-color: #3f3f3f`

2. PWA theme-color meta tag — change:
   `content="#ff8c00"`
   to:
   `content="#f0dfaf"`

These are the only two changes. Do not modify any script logic, PWA manifest references, or other HTML.

The body background must match the new `black` color constant (#3f3f3f) so there is no flash of wrong background before Elm renders. The theme-color should match the new amber accent.
  </action>
  <verify>
    <automated>grep -n "3f3f3f\|f0dfaf" /home/eelco/Source/elm/Tournaments/src/index.html</automated>
  </verify>
  <done>index.html contains `background-color: #3f3f3f` in body style and `content="#f0dfaf"` in theme-color meta. Build still succeeds.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `make build` exits 0 with no errors
2. `grep "0x3F 0x3F 0x3F" src/UI/Color.elm` returns the `black` definition
3. `grep "0xF0 0xDF 0xAF" src/UI/Color.elm` returns the `orange` definition
4. `grep "0xDC 0xDC 0xCC" src/UI/Color.elm` returns the `white` definition
5. `grep "3f3f3f" src/index.html` returns the body background style line
6. `grep "f0dfaf" src/index.html` returns the theme-color meta line
</verification>

<success_criteria>
- Build passes without errors
- UI/Color.elm: black=#3f3f3f, orange=#f0dfaf, white=#dcdccc, panel=#3a3a3a, primary=#2a2a2a, primaryDark=#353535, primaryLight=#555555, terminalBorder=#4f4f4f, terminalAccentDim=#cc9966
- index.html: body bg=#3f3f3f, theme-color=#f0dfaf
- All color consumers (View.elm, Style.elm, Form/, Results/) pick up changes automatically via named constants — no additional file edits required
</success_criteria>

<output>
After completion, create `.planning/phases/10-zenburn-color-scheme/10-01-SUMMARY.md`
</output>
