---
phase: 11-navigation-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/UI/Color.elm
  - src/UI/Button.elm
autonomous: true
requirements: [NAV-02]
gap_closure: true

must_haves:
  truths:
    - "Active nav link is clearly orange — visibly distinct from inactive links at a glance"
  artifacts:
    - path: "src/UI/Color.elm"
      provides: "activeNav color constant — saturated orange distinct from primaryText"
      contains: "activeNav"
    - path: "src/UI/Button.elm"
      provides: "navlink using Color.activeNav for active state"
      contains: "Color.activeNav"
  key_links:
    - from: "src/UI/Button.elm navlink (Active branch)"
      to: "UI.Color.activeNav"
      via: "Font.color Color.activeNav"
      pattern: "activeNav"
---

<objective>
Fix active nav link color so it is clearly saturated orange, visibly distinct from inactive links.

Purpose: UAT test 2 failed — Color.orange (0xF0 0xDF 0xAF) is a pale Zenburn amber nearly identical in luminance to Color.primaryText (0xDC 0xDC 0xCC). The active nav item needs a dedicated color with higher saturation and lower luminance so it pops on the dark background.
Output: Color.activeNav constant + navlink updated to use it.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-navigation-polish/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Color.activeNav and apply to navlink active state</name>
  <files>src/UI/Color.elm, src/UI/Button.elm</files>
  <action>
In src/UI/Color.elm:
- Add `activeNav` to the `exposing` list in the module declaration (keep alphabetical order, place between the existing entries).
- Add the constant below the existing `orange` definition:

```elm
activeNav : Color
activeNav =
    rgb255 0xF0 0xA0 0x30
```

This gives a saturated warm orange (hue ~35°, high saturation, luminance ~0.40) that is clearly distinct from Color.primaryText (0xDC 0xDC 0xCC, luminance ~0.68) on the Zenburn dark background.

In src/UI/Button.elm:
- In the `navlink` function, find the `Active` branch where `Font.color Color.orange` is applied.
- Replace `Color.orange` with `Color.activeNav` on that line only.
- Leave the `mouseOver [ Font.color Color.orange ]` on inactive links unchanged — that hover effect is intentionally the softer amber.
- No other changes.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make debug 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with zero errors. Active nav link renders in saturated orange (0xF0 0xA0 0x30); inactive links remain in primaryText color with amber hover.</done>
</task>

</tasks>

<verification>
Build must pass cleanly: `make debug` exits 0 with no Elm compiler errors.
Visually: active nav item is clearly a different, more saturated colour than inactive items when viewed on the dark Zenburn background.
</verification>

<success_criteria>
- Color.activeNav exists in UI/Color.elm and is exported
- navlink Active branch uses Color.activeNav (not Color.orange)
- make debug passes with no errors
- UAT gap "Active nav link is displayed in clear orange" is closed
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-polish/11-02-SUMMARY.md`
</output>
