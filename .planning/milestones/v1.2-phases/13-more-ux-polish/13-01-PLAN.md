---
phase: 13-more-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Activities.elm
  - src/UI/Style.elm
autonomous: true
requirements: [UX-POLISH-01, UX-POLISH-02, UX-POLISH-03]

must_haves:
  truths:
    - "Activities loading/not-asked state shows `[ ophalen... ]` instead of `Aan het ophalen.` / `Aan het ophalen...`"
    - "Activities success with empty list shows nothing (no empty state element)"
    - "Home page comment and author input fields have no visible label above them — the `>` prompt is the only identifier"
    - "All terminalInput fields (home inputs, participant form, blog post input) have a visibly lighter background (#353535) than the page body (#3F3F3F)"
  artifacts:
    - path: "src/Activities.elm"
      provides: "Loading state copy, label style fix"
    - path: "src/UI/Style.elm"
      provides: "terminalInput background color"
  key_links:
    - from: "src/Activities.elm viewActivities"
      to: "NotAsked/Loading branches"
      via: "Element.text"
      pattern: "ophalen"
    - from: "src/UI/Style.elm terminalInput"
      to: "Background.color"
      via: "Color.primaryDark"
      pattern: "Background\\.color"
---

<objective>
Polish three UX rough edges that involve text copy, label style, and input field discoverability in `Activities.elm` and `UI/Style.elm`.

Purpose: Users encounter "Aan het ophalen." which doesn't match the terminal aesthetic; input labels above fields (`ZEG WAT`, `NAAM`) contradict the established `>` prompt style; and all `terminalInput` fields are nearly invisible against the dark background.
Output: Updated `Activities.elm` (loading copy + hidden labels) and `UI/Style.elm` (lighter input bg).
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-more-ux-polish/13-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. -->

From src/Activities.elm (viewActivities, lines 65-79):
```elm
viewActivities : Length -> Time.Zone -> WebData (List Activity) -> Element.Element Msg
viewActivities wdth tz wActivities =
    case wActivities of
        NotAsked ->
            Element.text "Aan het ophalen."
        Loading ->
            Element.text "Aan het ophalen..."
        Failure _ ->
            UI.Text.error "Oeps. Daar ging iets niet goed."
        Success activities ->
            List.map (viewActivity tz) activities
                |> Element.column [ Screen.className "activities", width wdth, spacingXY 0 20, paddingXY 0 20 ]
```

From src/Activities.elm (viewCommentInput, the label usages):
- `commentInput` uses `label = UI.Text.labelText "ZEG WAT"` (Input.labelAbove — visible above-field header)
- `commentInputTrap` uses `label = UI.Text.labelText "ZEG WAT"` (same)
- `authorInput` uses `label = UI.Text.labelText "NAAM"` (same)
- Both already have `>` prompt rendered alongside the input

Both commentInput and authorInput already use `>` prefix pattern (lines ~168-218)

From src/UI/Style.elm (terminalInput, lines 627-646):
```elm
terminalInput : Bool -> List (Element.Attribute msg) -> List (Element.Attribute msg)
terminalInput hasError attrs =
    ...
    attrs
        ++ [ ...
           , Background.color Color.black   -- <-- change to Color.primaryDark
           , ...
           ]
```

From src/UI/Color.elm:
- `Color.black` = rgb255 0x3F 0x3F 0x3F  (page body background)
- `Color.primaryDark` = rgb255 0x35 0x35 0x35  (target input bg — slightly darker, giving visible lift)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Activities loading states and empty state</name>
  <files>src/Activities.elm</files>
  <action>
In `viewActivities`, update the `NotAsked` and `Loading` branches:
- `NotAsked ->` change `Element.text "Aan het ophalen."` to `Element.text "[ ophalen... ]"`
- `Loading ->` change `Element.text "Aan het ophalen..."` to `Element.text "[ ophalen... ]"` (same text for both states)
- `Success activities ->` add a guard: if `activities` is empty (`List.isEmpty activities`), return `Element.none`; otherwise the existing column expression

Keep the `Failure` branch untouched.

In `viewCommentInput`, replace all three `UI.Text.labelText` calls with `Input.labelHidden`:
- `commentInput`: change `label = UI.Text.labelText "ZEG WAT"` to `label = Input.labelHidden "ZEG WAT"`
- `commentInputTrap`: change `label = UI.Text.labelText "ZEG WAT"` to `label = Input.labelHidden "ZEG WAT"`
- `authorInput`: change `label = UI.Text.labelText "NAAM"` to `label = Input.labelHidden "NAAM"`

`Input.labelHidden` is already available via the `Element.Input as Input` import at the top of the file — no new imports needed.

Do NOT change the `>` prompt elements — those stay as-is. Do NOT touch `viewPostInput`.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes with no errors. `viewActivities` uses `[ ophalen... ]` for both NotAsked and Loading, returns `Element.none` for empty Success. `viewCommentInput` inputs have no visible label above them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix terminalInput background to primaryDark</name>
  <files>src/UI/Style.elm</files>
  <action>
In the `terminalInput` function (around line 641), change:
```
, Background.color Color.black
```
to:
```
, Background.color Color.primaryDark
```

`Color.primaryDark` is rgb255 0x35 0x35 0x35 — slightly darker than `Color.black` (0x3F 0x3F 0x3F), giving just enough contrast against the page body to reveal input fields are interactive. `Color.primaryDark` is already exported from `UI.Color` and imported in `UI.Style` via `import UI.Color as Color`.

Do NOT change `scoreInput` — that is a separate style and is explicitly excluded per user decision.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes. `terminalInput` now uses `Background.color Color.primaryDark`. All fields using `terminalInput` (home comment/author/post inputs, participant form inputs) have the lighter background.
  </done>
</task>

</tasks>

<verification>
After both tasks: `make build` completes with no errors or warnings. Visual check: navigate to `#home` in the browser — loading state shows `[ ophalen... ]`; inputs are subtly distinct from the page background; no `ZEG WAT`/`NAAM` label appears above comment/author fields.
</verification>

<success_criteria>
- `make build` exits 0
- `viewActivities` NotAsked and Loading both render `[ ophalen... ]`
- `Success []` renders nothing (Element.none)
- Comment and author inputs on home page have no above-field label
- All `terminalInput` styled fields show a #353535 background (vs #3F3F3F page body)
- `scoreInput` style unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/13-more-ux-polish/13-01-SUMMARY.md`
</output>
