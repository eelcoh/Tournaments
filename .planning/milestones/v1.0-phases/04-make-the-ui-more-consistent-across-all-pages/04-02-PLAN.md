---
phase: 04-make-the-ui-more-consistent-across-all-pages
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Activities.elm
  - src/Authentication.elm
autonomous: true
requirements:
  - CON-03

must_haves:
  truths:
    - "All Input.text and Input.multiline calls in Activities.elm use UI.Style.terminalInput styling"
    - "Both inputs in Authentication.elm use UI.Style.terminalInput styling"
    - "The inputs still function (onChange, label, placeholder wired correctly)"
    - "The codebase compiles after these changes"
  artifacts:
    - path: "src/Activities.elm"
      provides: "terminal-styled inputs in viewCommentInput and viewPostInput"
      contains: "terminalInput"
    - path: "src/Authentication.elm"
      provides: "terminal-styled username and password inputs"
      contains: "terminalInput"
  key_links:
    - from: "src/Activities.elm"
      to: "src/UI/Style.elm"
      via: "UI.Style.terminalInput call"
      pattern: "UI\\.Style\\.terminalInput"
    - from: "src/Authentication.elm"
      to: "src/UI/Style.elm"
      via: "UI.Style.terminalInput call"
      pattern: "UI\\.Style\\.terminalInput"
---

<objective>
Apply `UI.Style.terminalInput` styling to all bare `Input.text` and `Input.multiline` calls in `Activities.elm` (5 inputs) and `Authentication.elm` (2 inputs). These currently use ad-hoc `Border.rounded 0` with no consistent terminal aesthetic.

Purpose: Home page and authentication inputs match the terminal aesthetic already present in the Participant form card.

Output: Both files with terminalInput-styled inputs. No behavior changes — only visual styling updated.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Activities.elm
@src/Authentication.elm
@src/UI/Style.elm
@src/Form/Participant.elm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terminal inputs in Activities.elm</name>
  <files>src/Activities.elm</files>
  <action>
Replace all bare `Input.text`/`Input.multiline` attribute lists in `Activities.elm` with `UI.Style.terminalInput False [...]` calls. There are 5 inputs across two functions:

**In `viewCommentInput`:**

1. `commentInput` — currently `Input.multiline [ height (px 120), Border.rounded 0 ] area`
   Replace with: `Input.multiline (UI.Style.terminalInput False [ height (px 120), width fill ]) area`

2. `commentInputTrap` — currently `Input.text [ Events.onFocus ShowCommentInput, height (px 48), Border.rounded 0 ] area`
   Replace with: `Input.text (UI.Style.terminalInput False [ Events.onFocus ShowCommentInput, height (px 48), width fill ]) area`

3. `authorInput` — currently `Input.text [ height (px 48), Border.rounded 0 ] area`
   Replace with: `Input.text (UI.Style.terminalInput False [ height (px 48), width fill ]) area`

**In `viewPostInput`:**

4. `titleInput` — currently `Input.text [ height (px 36), Border.rounded 0 ] area`
   Replace with: `Input.text (UI.Style.terminalInput False [ height (px 36), width fill ]) area`

5. `postInput` — currently `Input.multiline [ height (px 200), Border.rounded 0 ] area`
   Replace with: `Input.multiline (UI.Style.terminalInput False [ height (px 200), width fill ]) area`

6. `postInputTrap` — currently `Input.text [ Events.onFocus ShowPostInput, height (px 36), Border.rounded 0 ] area`
   Replace with: `Input.text (UI.Style.terminalInput False [ Events.onFocus ShowPostInput, height (px 36), width fill ]) area`

7. `passphraseInput` — currently `Input.text [ height (px 36), Border.rounded 0 ] area`
   Replace with: `Input.text (UI.Style.terminalInput False [ height (px 36), width fill ]) area`

8. `authorInput` in viewPostInput — currently `Input.text [ height (px 36), Border.rounded 0 ] area`
   Replace with: `Input.text (UI.Style.terminalInput False [ height (px 36), width fill ]) area`

Remove `Border.rounded 0` from each attr list (terminalInput already sets this via its own attributes — avoid duplication, though duplication is harmless in elm-ui, removal keeps the code clean).

Ensure `UI.Style` is already imported (it is — check the import list). Remove `Element.Border as Border` import only if no other use of `Border` remains; do NOT remove it if other Border calls exist elsewhere in the file (e.g., `Border.color`, `Border.widthEach`).

Reference implementation: `src/Form/Participant.elm` — examine how `UI.Style.terminalInput hasError [...]` is used there for confirmation of the pattern.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make debug 2>&1 | tail -5</automated>
    <manual>Confirm no compilation errors. Grep: grep -c "terminalInput" src/Activities.elm — should be >= 5</manual>
  </verify>
  <done>All Input.text/multiline calls in Activities.elm use UI.Style.terminalInput. make debug compiles without errors. Border.rounded 0 removed from affected input attribute lists.</done>
</task>

<task type="auto">
  <name>Task 2: Terminal inputs in Authentication.elm</name>
  <files>src/Authentication.elm</files>
  <action>
Replace both bare input attribute lists in `Authentication.elm` with `UI.Style.terminalInput False [...]`:

1. `username` function — currently `Input.multiline [ Events.onFocus ShowCommentInput, height (px 36) ] area`
   Replace with: `Input.multiline (UI.Style.terminalInput False [ Events.onFocus ShowCommentInput, height (px 48), width fill ]) area`
   Note: bump height from 36px to 48px — more comfortable touch target for a single-line username field. The `multiline` type is kept as-is (it is used for spellcheck=True).

2. `password` function — currently `Input.text [ height (px 36) ] area`
   Replace with: `Input.text (UI.Style.terminalInput False [ height (px 48), width fill ]) area`
   Note: bump height from 36px to 48px for consistency with other terminal inputs.

Add `UI.Style` to the import list if not already present. It IS already imported — check the current imports in the file. No new imports needed.

The outer `Element.column (UI.Style.darkBox [...])` wrapper in `view` is already correct — leave it unchanged.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make debug 2>&1 | tail -5</automated>
    <manual>Confirm no compilation errors. Grep: grep -c "terminalInput" src/Authentication.elm — should be 2</manual>
  </verify>
  <done>Both inputs in Authentication.elm use UI.Style.terminalInput. Heights normalized to 48px. make debug compiles without errors.</done>
</task>

</tasks>

<verification>
Run `make debug` after both tasks. Then verify:
- `grep -c "terminalInput" src/Activities.elm` returns >= 5
- `grep -c "terminalInput" src/Authentication.elm` returns 2
- No `Border.rounded 0` remains in the replaced input attrs (may still appear elsewhere in Activities.elm — only remove from input attribute lists)
</verification>

<success_criteria>
- All Input.text/Input.multiline calls in Activities.elm and Authentication.elm use UI.Style.terminalInput
- make debug compiles without errors
- Input behavior (onChange, labels, placeholders) is unchanged — only attrs list modified
- Heights normalized to 48px for consistency
</success_criteria>

<output>
After completion, create `.planning/phases/04-make-the-ui-more-consistent-across-all-pages/04-02-SUMMARY.md`
</output>
