---
phase: 05-bug-fixes-and-ux-polish
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified: []
autonomous: false
requirements: [ISSUE-1, ISSUE-2, ISSUE-3, ISSUE-4, ISSUE-5]

must_haves:
  truths:
    - "All 5 issues are visually verified as fixed by the user"
    - "make build succeeds with no compiler errors"
  artifacts:
    - path: "src/Form/Bracket/View.elm"
      provides: "All bracket fixes verified"
    - path: "src/Form/Topscorer.elm"
      provides: "Topscorer terminal aesthetic verified"
    - path: "src/Activities.elm"
      provides: "Comment input terminal styling verified"
    - path: "src/Form/GroupMatches.elm"
      provides: "Scroll wheel boundary height verified"
  key_links: []
---

<objective>
Human verification checkpoint: visually confirm all 5 bug fixes and UX polish changes work correctly in the browser.

Purpose: All 5 fixes involve visual/interactive behavior that requires human eyes to confirm — compiler success (make build) is necessary but not sufficient.
Output: User sign-off on all 5 issues.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and serve the app</name>
  <files></files>
  <action>
Run `make build` to produce build/main.js. Confirm zero Elm compiler errors.

Output the command to serve locally:
```
python3 -m http.server --directory build
```

Then instruct the user to open http://localhost:8000 in their browser and verify each of the 5 issues below.
  </action>
  <verify>
    <automated>make build 2>&1 | tail -5</automated>
  </verify>
  <done>make build exits with code 0. build/main.js and supporting assets present in build/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all 5 fixes</name>
  <what-built>
All 5 Phase 5 bug fixes:
1. Flag images in bracket wizard computer-layout team cells (viewTeamBadge)
2. Terminal styling on home page comment input with '>' prompt prefix
3. Sticky "Ga verder" button + ✓ checkmarks in bracket stepper when R32 is complete
4. Topscorer page: vertical terminal text rows with '>' prefix, flag images, '--- TITLE ---' headers
5. GroupBoundary rows have same 44px height as match rows (no vertical jump at group boundaries)
  </what-built>
  <how-to-verify>
Serve the app with `python3 -m http.server --directory build` and open http://localhost:8000.

**Issue #1 — Flags in bracket computer layout:**
- On a desktop viewport (>500px wide), navigate to the bracket wizard
- In any round section's grid (the per-group rows), each team cell should show a small flag icon to the left of the 3-char team code
- Previously: only text "NED", "ARG", etc. were visible

**Issue #2 — Home page comment input:**
- Navigate to the home page (# or #home)
- The comment input field should have: underline-only border, dark background
- Type in the field: border/underline should turn orange on focus
- A '>' character should appear to the left of the input field at all times

**Issue #3 — Sticky bracket completion button + ✓ stepper:**
- Navigate to the bracket wizard and fill in the R32 round (32 teams)
- The "Ga verder →" button should appear pinned to the bottom of the screen
- Completed rounds in the ASCII stepper (both full and compact views) should show ✓ instead of x
- Navigate away from the bracket page — the sticky button should NOT appear on group matches or topscorer

**Issue #4 — Topscorer terminal aesthetic:**
- Navigate to the topscorer page
- Teams should appear as a vertical list of text rows (one per line), NOT as badge buttons
- Each team row should show flag + 3-char code
- Click a team: that team gets '> ' prefix in orange, others are plain
- A player list appears as vertical rows below "--- KIES EEN SPELER ---" header
- Selected player gets '> ' prefix

**Issue #5 — Group boundary height in scroll wheel:**
- Navigate to group matches
- Scroll to the last match of Group A (or any group)
- Use scroll wheel, keyboard, or swipe to advance past the last match
- The boundary label '-- B --' should appear at the same height as match rows
- The scroll wheel column should NOT jump vertically when a boundary label appears or disappears
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which issue(s) still need fixing.</resume-signal>
  <action>Human verification — no automated action. User manually visits the app at http://localhost:8000 and checks each of the 5 issues listed in how-to-verify.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; visual confirmation is required</automated>
    <manual>See how-to-verify above — user confirms each of the 5 issue fixes visually in the browser.</manual>
  </verify>
  <done>User types "approved" confirming all 5 visual checks pass.</done>
  <files>none</files>
</task>

</tasks>

<verification>
User types "approved" after visual verification of all 5 issues.
</verification>

<success_criteria>
- make build exits 0
- User approves all 5 visual/interactive checks
</success_criteria>

<output>
After completion, create `.planning/phases/05-bug-fixes-and-ux-polish/05-04-SUMMARY.md` following the summary template.
</output>
