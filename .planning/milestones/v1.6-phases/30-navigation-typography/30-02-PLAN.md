---
phase: 30-navigation-typography
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/View.elm
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
gap_closure: true

must_haves:
  truths:
    - "The app header logo row is exactly 44px tall — verified by Element.height (Element.px 44) on the logo row element"
    - "Nav links remain fully visible below the logo row — not clipped by the 44px constraint"
    - "Build compiles with no errors"
  artifacts:
    - path: "src/View.elm"
      provides: "Header with 44px logo row + separate nav link row"
      contains: "Element.px 44"
  key_links:
    - from: "src/View.elm"
      to: "links column (logo row)"
      via: "Element.height (Element.px 44)"
      pattern: "px 44"
---

<objective>
Close the single NAV-01 gap: restructure the app header so the logo sits in a dedicated 44px-tall row, satisfying the prototype spec, while nav links remain in a separate row below and are never clipped.

Purpose: The verification report found that Plan 01 correctly applied typography and background/border but left out the 44px height constraint to avoid clipping nav links. This plan fixes that with a two-row restructure.
Output: Updated src/View.elm with a 44px logo row + a separate nav link row, both within the existing dark-background column.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-navigation-typography/30-01-SUMMARY.md
@src/View.elm
</context>

<interfaces>
<!-- Current links column structure (src/View.elm lines 165-184) after Plan 01 execution. -->
<!-- Read by executor — no codebase exploration needed. -->

```elm
links =
    Element.column
        [ Element.width Element.fill
        , Background.color (Element.rgb255 0x2B 0x2B 0x2B)
        , Border.widthEach { bottom = 1, top = 0, left = 0, right = 0 }
        , Border.color Color.terminalBorder
        ]
        [ Element.el
            [ Element.Events.onClick TitleTap
            , Element.pointer
            , Font.color Color.orange
            , UI.Font.mono
            , Font.size 12
            , Font.letterSpacing 0.1
            , Element.paddingXY 0 8
            ]
            (Element.text "Voetbalpool")
        , Element.wrappedRow [ Element.paddingXY 0 8, Element.spacing 4 ]
            (List.map link linkList)
        ]
```

Target structure after this plan:

```elm
links =
    Element.column
        [ Element.width Element.fill
        , Background.color (Element.rgb255 0x2B 0x2B 0x2B)
        , Border.widthEach { bottom = 1, top = 0, left = 0, right = 0 }
        , Border.color Color.terminalBorder
        ]
        [ Element.row
            [ Element.width Element.fill
            , Element.height (Element.px 44)
            ]
            [ Element.el
                [ Element.Events.onClick TitleTap
                , Element.pointer
                , Font.color Color.orange
                , UI.Font.mono
                , Font.size 12
                , Font.letterSpacing 0.1
                , Element.paddingXY 0 8
                ]
                (Element.text "Voetbalpool")
            ]
        , Element.wrappedRow [ Element.paddingXY 0 8, Element.spacing 4 ]
            (List.map link linkList)
        ]
```

Key change: wrap the logo `Element.el` in an `Element.row` with `Element.height (Element.px 44)`.
The nav `wrappedRow` stays as a sibling in the outer column — fully visible, no height constraint.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure header to enforce 44px logo row (NAV-01)</name>
  <files>src/View.elm</files>
  <action>
In `src/View.elm`, find the `links` binding (around line 165). The current structure is an `Element.column` with two direct children: a logo `Element.el` and a nav `Element.wrappedRow`.

Make exactly one structural change: wrap the logo `Element.el` in an `Element.row` that has `Element.height (Element.px 44)`. The nav `wrappedRow` remains as the second child of the outer column, unchanged.

Before (the two children of the outer column):
```elm
Element.el
    [ Element.Events.onClick TitleTap
    , Element.pointer
    , Font.color Color.orange
    , UI.Font.mono
    , Font.size 12
    , Font.letterSpacing 0.1
    , Element.paddingXY 0 8
    ]
    (Element.text "Voetbalpool")
, Element.wrappedRow [ Element.paddingXY 0 8, Element.spacing 4 ]
    (List.map link linkList)
```

After (same two children, but logo wrapped in a 44px row):
```elm
Element.row
    [ Element.width Element.fill
    , Element.height (Element.px 44)
    ]
    [ Element.el
        [ Element.Events.onClick TitleTap
        , Element.pointer
        , Font.color Color.orange
        , UI.Font.mono
        , Font.size 12
        , Font.letterSpacing 0.1
        , Element.paddingXY 0 8
        ]
        (Element.text "Voetbalpool")
    ]
, Element.wrappedRow [ Element.paddingXY 0 8, Element.spacing 4 ]
    (List.map link linkList)
```

Do not change any other attributes of the outer column (background, border, width) or the nav wrappedRow. Do not add `Element.clipY` or any height constraint to the outer column.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make debug 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with no Elm compiler errors. `src/View.elm` contains `Element.px 44` on the logo row. The `wrappedRow` for nav links is still present as a sibling, not nested inside the 44px row.</done>
</task>

</tasks>

<verification>
- `make debug` passes with no compiler errors
- `grep "px 44" src/View.elm` matches exactly one line (the logo row height)
- `grep "wrappedRow" src/View.elm` confirms the nav link row is still present as a sibling of the logo row, not inside it
- The outer column retains `Background.color (Element.rgb255 0x2B 0x2B 0x2B)`, `Border.widthEach { bottom = 1 ... }`, and `Border.color Color.terminalBorder`
</verification>

<success_criteria>
- NAV-01 fully satisfied: logo row is exactly 44px tall (enforced by elm-ui layout engine)
- Nav links remain unclipped in a separate row below the logo
- No regression: background, bottom border, font size 12, letter-spacing 0.1 all still present
- `make debug` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/30-navigation-typography/30-02-SUMMARY.md`
</output>
