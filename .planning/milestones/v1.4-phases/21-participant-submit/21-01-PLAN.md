---
phase: 21-participant-submit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Form/Participant.elm
autonomous: true
requirements:
  - FORM-07
must_haves:
  truths:
    - "Each participant field row has an uppercase label rendered above a bordered container"
    - "The bordered container holds a '>' prompt and the input inline on one row"
    - "The focused field's container border turns orange; unfocused fields show terminalBorder grey"
    - "Fields with errors show a red border and '!' prompt instead of orange '>' "
    - "All 6 fields (Naam, Adres, Woonplaats, Email, Telefoonnummer, Hoe ken je ons?) are present with dimmed placeholder text"
  artifacts:
    - path: "src/Form/Participant.elm"
      provides: "Restyled participant field rows with bordered containers and uppercase labels"
      contains: "Font.letterSpacing"
  key_links:
    - from: "src/Form/Participant.elm"
      to: "Form.Participant.Types"
      via: "FocusField/BlurField msgs drive state.activeField which drives container border color"
      pattern: "FocusField|activeField"
    - from: "src/Form/Participant.elm"
      to: "UI.Style.terminalInput"
      via: "inner input still uses terminalInput; outer container border driven by parent state"
      pattern: "terminalInput"
---

<objective>
Restyle the participant card field rows to match the prototype visual language. Each field gets an uppercase label above a bordered container; the container holds a `>` prompt and the input inline. The container border turns orange when the field is focused, red on error, and dim grey otherwise.

Purpose: Satisfies FORM-07 — consistent terminal-style field rows matching all other v1.4 form cards.
Output: Modified `src/Form/Participant.elm` with new `inputField` layout.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-participant-submit/21-CONTEXT.md

@src/Form/Participant.elm
@src/Form/Participant/Types.elm
@src/UI/Style.elm
@src/UI/Color.elm
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/Form/Participant/Types.elm:
```elm
type alias State =
    { activeField : Maybe FieldTag }

type FieldTag
    = NameTag | PostalTag | ResidenceTag | EmailTag | PhoneTag | KnowsTag

type Msg
    = Set Attr
    | FocusField FieldTag
    | BlurField
```

From src/UI/Color.elm (relevant colors):
- `Color.orange` — rgb255 0xF0 0xDF 0xAF — focus border
- `Color.red` — rgb255 200 0 0 — error border / prompt
- `Color.grey` — rgb255 110 110 110 — default dim label / border / prompt
- `Color.terminalBorder` — dim grey border (same as v1.4 tile borders throughout)
- `Color.black` — rgb255 0x3F 0x3F 0x3F — dark background

From UI.Style.terminalInput:
- Existing function: `terminalInput : Bool -> List (Element.Attribute msg) -> List (Element.Attribute msg)`
- Returns underline-only bottom border, orange text/border, dark bg, with `Element.focused` override
- New bordered container wraps around this; inner `terminalInput` stays as-is for the actual input element

Phase 20 established pattern (Form.Topscorer search bar):
- Outer container: `Border.width 1`, `Border.color (if isFocused then Color.orange else Color.terminalBorder)`
- Inner input: `Html.Events.onFocus MsgVariant`, `Html.Events.onBlur MsgVariant`
- Parent state field drives container border color
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle participant field rows with bordered containers and uppercase labels</name>
  <files>src/Form/Participant.elm</files>
  <action>
Rewrite the `inputField` helper function inside `view` in `src/Form/Participant.elm`. The existing layout (prompt+label row above indented input) becomes a two-part vertical stack:

1. **Uppercase label** (above the container):
   - `Element.el` with `Font.size 9` (or use `UI.Font.scaled -1` — whichever gets closest to 9px), `Font.color Color.grey`, `Font.letterSpacing 0.14`, `UI.Font.mono`
   - Text: `String.toUpper lbl`

2. **Bordered container** (holds prompt + input inline):
   - `Element.row` with `Border.width 1`, `Border.color borderColor`, `Element.padding 8`, `width fill`, `Element.spacing 8`
   - `borderColor`: `Color.red` when `hasError`, `Color.orange` when `isActive` (i.e. `state.activeField == Just tag`), else `Color.terminalBorder`
   - Background: `Background.color Color.black` (dark bg matching other v1.4 containers)
   - First child: `Element.el [Font.color promptColor, UI.Font.mono, Element.width (px 12)] (Element.text promptChar)`
     - `promptChar`: `"!"` on error, `">"` when active, `"-"` otherwise
     - `promptColor`: `Color.red` on error, `Color.orange` when active, `Color.grey` otherwise
   - Second child: `Element.Input.text` using `UI.Style.terminalInput hasError [width fill, Element.htmlAttribute (Html.Events.onFocus (FocusField tag)), Element.htmlAttribute (Html.Events.onBlur BlurField)]` — keep existing terminalInput call unchanged

3. **Placeholder text**: Update `inp` record to include `placeholder = Just (Element.Input.placeholder [Font.color Color.grey, UI.Font.mono] (Element.text placeholderText))`. Add a `placeholderText` let-binding mapping each tag to its string: NameTag → "jouw naam", PostalTag → "adres", ResidenceTag → "woonplaats", EmailTag → "naam@voorbeeld.nl", PhoneTag → "+31 6 ...", KnowsTag → "hoe ken je ons?"

4. **Label list**: Update `labels` to `[ "NAAM", "ADRES", "WOONPLAATS", "EMAIL", "TELEFOONNUMMER", "HOE KEN JE ONS?" ]` — uppercase (or use `String.toUpper` in the rendering and keep labels in sentence case as they are; prefer `String.toUpper lbl` in the view so labels remain readable in source).

5. **Outer column**: Each field is `Element.column [Element.spacing 4, width fill] [label, borderedContainer]`. The overall list is `Element.column [Element.spacing 12, width fill] lines` — increase spacing between fields to 12 for visual separation.

Add `Element.Background as Background` import if not already present (check existing imports). Add `import Element.Background as Background` if missing.

Do NOT change `update`, `isComplete`, or the `keys`/`fieldTags` lists. Do NOT change `Form.Participant.Types`.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make debug 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `make debug` exits 0 with no Elm compilation errors
    - Each of the 6 participant fields renders as: uppercase label above a bordered container with `>` prompt + input inline
    - Active field container border is orange; unfocused fields show grey border; error fields show red border
    - Placeholder text appears in each field when empty
  </done>
</task>

</tasks>

<verification>
Run `make debug` from the project root — zero compilation errors required.

Visual check: navigate to the participant card in the form. Each of the 6 fields should show an uppercase label (e.g. "NAAM") above a bordered dark box containing a `>` prompt and an input. Click/tab into a field — its border turns orange. Enter invalid email — border turns red with `!` prompt.
</verification>

<success_criteria>
- `make debug` passes with no errors
- Participant field rows visually match the prototype bordered container pattern from v1.4
- FORM-07 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/21-participant-submit/21-01-SUMMARY.md`
</output>
