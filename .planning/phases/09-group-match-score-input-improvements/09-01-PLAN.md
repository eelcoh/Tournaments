---
phase: 09-group-match-score-input-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/UI/Button/Score.elm
autonomous: true
requirements: []

must_haves:
  truths:
    - "Tapping a score keyboard button does not trigger browser text selection"
    - "Score keyboard buttons remain fully clickable and fire their Msg as before"
  artifacts:
    - path: "src/UI/Button/Score.elm"
      provides: "user-select: none CSS attributes on score buttons"
      contains: "user-select"
  key_links:
    - from: "src/UI/Button/Score.elm"
      to: "scoreButton_"
      via: "Element.htmlAttribute (Html.Attributes.style)"
      pattern: "user-select"
---

<objective>
Add `user-select: none` (and `-webkit-user-select: none` for Safari) to the score keyboard buttons so that tapping them on mobile does not trigger browser text selection.

Purpose: Score keyboard buttons currently cause an unwanted text selection highlight on touch, which feels broken. This is a one-line-per-button CSS fix.
Output: Modified `src/UI/Button/Score.elm` with no-select attributes on `scoreButton_`.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key types from src/UI/Button/Score.elm that the executor needs -->

```elm
-- scoreButton_ is the leaf rendering function for every score button
scoreButton_ : ButtonSemantics -> msg -> String -> Element msg
scoreButton_ semantics msg buttonText =
    let
        buttonLayout =
            UI.Style.scoreButton semantics
                [ w, height (px 44), onClick msg, centerX, centerY ]
    in
    Element.row buttonLayout [ text buttonText ]

-- viewKeyboard is the keyboard container (column of rows of buttons)
viewKeyboard : msg -> (Int -> Int -> msg) -> Element msg
viewKeyboard noop matchToMsg = ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add user-select: none to score keyboard buttons</name>
  <files>src/UI/Button/Score.elm</files>
  <action>
In `scoreButton_`, add two `Element.htmlAttribute` calls to the `buttonLayout` attribute list:

```elm
Element.htmlAttribute (Html.Attributes.style "user-select" "none")
Element.htmlAttribute (Html.Attributes.style "-webkit-user-select" "none")
```

Place them at the end of the attribute list passed to `UI.Style.scoreButton semantics [...]` (alongside `w`, `height (px 44)`, `onClick msg`, `centerX`, `centerY`).

The `Html.Attributes` module is already imported at the top of `Form/GroupMatches.elm` but check whether it is imported in `UI/Button/Score.elm`. If not, add:
`import Html.Attributes`

Do NOT add these attributes to `viewKeyboard` container — apply at the leaf `scoreButton_` level so each individual button cell is non-selectable.

Do not change any other logic, Msg types, or layout.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make debug 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes with no errors. The two `user-select` style attributes appear on `scoreButton_` in `src/UI/Button/Score.elm`. Safari (-webkit) variant is also present.</done>
</task>

</tasks>

<verification>
`make debug` completes without Elm compiler errors. Grep confirms both `user-select` and `-webkit-user-select` are present in `src/UI/Button/Score.elm`.
</verification>

<success_criteria>
- `make debug` passes
- `src/UI/Button/Score.elm` contains `Html.Attributes.style "user-select" "none"` and `Html.Attributes.style "-webkit-user-select" "none"` in `scoreButton_`
- No other logic or layout is altered
</success_criteria>

<output>
After completion, create `.planning/phases/09-group-match-score-input-improvements/09-01-SUMMARY.md`
</output>
