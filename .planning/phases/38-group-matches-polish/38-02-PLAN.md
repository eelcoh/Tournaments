---
phase: 38-group-matches-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - src/Form/GroupMatches.elm
autonomous: false
requirements:
  - GMATCH-07
  - GMATCH-08
  - GMATCH-09
  - GMATCH-10

must_haves:
  truths:
    - "Group label separators in the scroll wheel match the R32 bracket group code style"
    - "Group nav letters use Font.size 12 and mono font matching R32 headers"
    - "Andere score link displays at Font.size 12"
    - "Match counter displays at Font.size 12"
  artifacts:
    - path: "src/Form/GroupMatches.elm"
      provides: "Typography consistency with bracket R32 group headers"
      contains: "Color.terminalAccentDim"
  key_links:
    - from: "src/Form/GroupMatches.elm"
      to: "src/UI/Color.elm"
      via: "Color.terminalAccentDim token"
      pattern: "Color\\.terminalAccentDim"
---

<objective>
Apply consistent typography to group label separators, group nav, "andere score" link, and match counter — matching the R32 bracket group code header style.

Purpose: The scroll wheel currently uses inconsistent font sizes (inherited/implicit) and `Color.grey` for group labels instead of `Color.terminalAccentDim`. This plan aligns these elements with the bracket's R32 section for visual consistency.
Output: Visually consistent typography across the group matches card.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-group-matches-polish/38-RESEARCH.md
@.planning/phases/38-group-matches-polish/38-01-SUMMARY.md

<interfaces>
<!-- R32 group code header token set from src/Form/Bracket/View.elm -->
```elm
Element.el
    [ Font.color Color.terminalAccentDim
    , UI.Font.mono
    , Font.size 12
    , Element.width (Element.px 24)
    , Element.height (Element.px 44)
    ]
    (UI.Text.allCenteredText (Group.toString grp))
```

<!-- Current group label in viewWindowLine (to be changed) -->
```elm
Element.el
    [ centerX, Font.color Color.grey, UI.Font.mono, Element.height (Element.px 44), centerY ]
    (groupLabel grp)
```

<!-- Current viewGroupNav letter styling (to be changed) -->
```elm
Element.el [ Font.color clr, UI.Font.mono, centerY ] (Element.text label)
```

<!-- Current viewProgress (to be changed) -->
```elm
Element.el [ centerX, Font.color Color.grey, UI.Font.mono ] (Element.text ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply R32 group header style to group labels, nav, and small text elements</name>
  <files>src/Form/GroupMatches.elm</files>
  <read_first>
    - src/Form/GroupMatches.elm (current state after Plan 01)
    - src/Form/Bracket/View.elm (reference: search for "terminalAccentDim" and "Font.size 12" to see R32 group label style)
    - src/UI/Color.elm (verify Color.terminalAccentDim exists)
  </read_first>
  <action>
**Change 1: Group label separators in `viewWindowLine` (GMATCH-07)**

Find the `WLGroupLabel grp ->` branch in `viewWindowLine`. Change:
```elm
WLGroupLabel grp ->
    Element.el
        [ centerX
        , Font.color Color.grey
        , UI.Font.mono
        , Element.height (Element.px 44)
        , centerY
        ]
        (groupLabel grp)
```
To:
```elm
WLGroupLabel grp ->
    Element.el
        [ centerX
        , Font.color Color.terminalAccentDim
        , UI.Font.mono
        , Font.size 12
        , Element.height (Element.px 44)
        , centerY
        ]
        (groupLabel grp)
```

Two changes: `Color.grey` becomes `Color.terminalAccentDim`, and `Font.size 12` is added.

Also update the `WLEndMarker` branch to match (same style for consistency):
```elm
WLEndMarker ->
    Element.el
        [ centerX
        , Font.color Color.terminalAccentDim
        , UI.Font.mono
        , Font.size 12
        , Element.height (Element.px 44)
        , centerY
        ]
        (Element.text "-- END --")
```

**Change 2: Group nav letters in `viewGroupNav` (GMATCH-08)**

Find the inner `Element.el` in `viewGroupLetter` that renders the letter text. Change:
```elm
(Element.el
    [ Font.color clr
    , UI.Font.mono
    , centerY
    ]
    (Element.text label)
)
```
To:
```elm
(Element.el
    [ Font.color clr
    , UI.Font.mono
    , Font.size 12
    , centerY
    ]
    (Element.text label)
)
```

One change: add `Font.size 12`. The color logic (`Color.orange` for active, `Color.green` for complete, `Color.grey` for pending) stays unchanged.

**Change 3: "Andere score" link in `view` (GMATCH-09)**

Find `andereScoreLink` in the `view` function. Change:
```elm
andereScoreLink =
    Element.el
        (UI.Style.link [ centerX, UI.Font.mono, Element.Events.onClick ShowManualInput ])
        (Element.text "andere score")
```
To:
```elm
andereScoreLink =
    Element.el
        (UI.Style.link [ centerX, UI.Font.mono, Font.size 12, Element.Events.onClick ShowManualInput ])
        (Element.text "andere score")
```

One change: add `Font.size 12` to the attribute list inside `UI.Style.link`.

**Change 4: Match counter in `viewProgress` (GMATCH-10)**

Find `viewProgress`. Change:
```elm
Element.el [ centerX, Font.color Color.grey, UI.Font.mono ]
    (Element.text (String.fromInt completed ++ "/" ++ String.fromInt total))
```
To:
```elm
Element.el [ centerX, Font.color Color.grey, UI.Font.mono, Font.size 12 ]
    (Element.text (String.fromInt completed ++ "/" ++ String.fromInt total))
```

One change: add `Font.size 12`.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make debug 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/Form/GroupMatches.elm `WLGroupLabel` branch contains `Font.color Color.terminalAccentDim`
    - src/Form/GroupMatches.elm `WLGroupLabel` branch contains `Font.size 12`
    - src/Form/GroupMatches.elm `WLEndMarker` branch contains `Font.color Color.terminalAccentDim`
    - src/Form/GroupMatches.elm `viewGroupLetter` inner el contains `Font.size 12`
    - src/Form/GroupMatches.elm `andereScoreLink` contains `Font.size 12`
    - src/Form/GroupMatches.elm `viewProgress` contains `Font.size 12`
    - `make debug` exits with code 0 (no compiler errors)
  </acceptance_criteria>
  <done>
    - Group label separators use Color.terminalAccentDim and Font.size 12 (matching R32 bracket headers)
    - Group nav letters use Font.size 12 with mono font
    - "Andere score" link displays at Font.size 12
    - Match counter (N/M) displays at Font.size 12
    - Build compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all group matches polish</name>
  <what-built>Responsive team badges with mirrored layout, consistent column alignment, and R32-matching typography across the entire group matches scroll wheel card</what-built>
  <how-to-verify>
    1. Run `cd /home/eelco/Source/elm/Tournaments && make debug && python3 -m http.server --directory build 8000`
    2. Open http://localhost:8000 on a phone or in Chrome DevTools responsive mode
    3. Navigate to the form and reach the group matches card

    **Badge responsiveness (GMATCH-01):**
    4. At width >=400px: badges should be 150px wide showing flag + full team name (e.g., "Netherlands")
    5. At width <400px: badges should be 85px wide showing flag + 3-letter code (e.g., "NED")

    **Badge mirroring (GMATCH-02/03/04/05):**
    6. Home team (left): flag on left, name/code on right
    7. Away team (right): name/code on left, flag on right (mirrored)

    **Column alignment (GMATCH-06):**
    8. Scroll through several matches -- all rows should be perfectly aligned (no ragged baselines)
    9. Score column should be centered between the two badges

    **Group labels (GMATCH-07):**
    10. Group separator lines (e.g., "-- A --") should use a dim accent color (not grey) and 12px mono font

    **Group nav (GMATCH-08):**
    11. Group letter navigation (A B C ... L) should use 12px mono font

    **Small text (GMATCH-09/10):**
    12. "andere score" link should appear as a small 12px styled button
    13. Match counter (e.g., "0/36") should appear at 12px
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `make debug` compiles without errors
- Group labels use `Color.terminalAccentDim` and `Font.size 12`
- Group nav uses `Font.size 12`
- "Andere score" uses `Font.size 12`
- Match counter uses `Font.size 12`
- Visual checkpoint confirms all 10 requirements
</verification>

<success_criteria>
- All 4 typography requirements (GMATCH-07 through GMATCH-10) addressed with explicit Font.size 12 and correct color tokens
- Visual checkpoint confirms the complete group matches card looks polished and consistent with the bracket
</success_criteria>

<output>
After completion, create `.planning/phases/38-group-matches-polish/38-02-SUMMARY.md`
</output>
