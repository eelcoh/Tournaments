---
phase: 04-make-the-ui-more-consistent-across-all-pages
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
autonomous: false
files_modified: []
requirements:
  - CON-01
  - CON-02
  - CON-03
  - CON-04
  - CON-05

must_haves:
  truths:
    - "Activity feed displays correctly on both Phone and Computer viewports"
    - "Comment/post input boxes have terminal styling (underline border, dark bg)"
    - "Authentication form inputs have terminal styling"
    - "Results pages are width-constrained and do not stretch full-width on a 1280px desktop"
    - "Ranking rows and topscorer rows are clickable with correct hover styling"
    - "Spacing between sections is consistent across all Results pages"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm the full UI consistency pass looks correct across all pages in both Phone (375px) and Computer (1280px) viewports.

Purpose: UI changes in plans 01-03 are purely visual — only a human can confirm they look right.

Output: User approval or list of issues to address.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and serve</name>
  <files></files>
  <action>
Run `make build` to produce the production build, then start a local server:

```bash
cd /home/eelco/Source/elm/Tournaments && make build && python3 -m http.server 8080 --directory build
```

If port 8080 is in use, try 8081. Report the URL to the user.
  </action>
  <verify>
    <automated>cd /home/eelco/Source/elm/Tournaments && make build 2>&1 | tail -5</automated>
    <manual>Build succeeds and server starts</manual>
  </verify>
  <done>make build succeeds. Server running at http://localhost:8080</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification across all pages</name>
  <action>Pause for human to verify the visual changes across all pages in both viewport sizes.</action>
  <what-built>
Full UI consistency pass across all pages:
- UI.Page.container applied to all 5 Results pages (Matches, Knockouts, Topscorers, Ranking, Bets)
- UI.Button.dataRow for Ranking rows and Topscorer rows (replaces raw onClick)
- UI.Style.terminalInput on all Activities.elm inputs (comment + post forms)
- UI.Style.terminalInput on Authentication.elm username + password inputs
- Spacing normalized to 3-tier rhythm: 24px sections / 16px items / 8px tight
  </what-built>
  <how-to-verify>
Open http://localhost:8080 and test the following:

**Phone viewport (375px) — use browser DevTools responsive mode:**

1. Navigate to Home (#home) — Activity feed should show log-line format. Comment input box should have underline-only border (terminal style), dark background. Click the comment input to expand — both the message textarea and author field should have the same terminal styling.
2. Navigate to Stand (#stand) — Ranking should be width-constrained (not full width). Ranking rows should show orange hover color when you hover over a name.
3. Navigate to Resultaten (#uitslagen) — Match cards should have consistent spacing and not be clipped.
4. Navigate to Topscorers — list should be width-constrained. Each topscorer row should highlight on hover.

**Computer viewport (1280px) — restore to desktop:**

5. Home — comment/post input boxes should still look correct (terminal style, not stretched to full 1280px width, respects the Screen.maxWidth constraint).
6. Stand — ranking groups should be max-width constrained (not stretching to 1280px full width).
7. All Results pages — content should be visually centered with consistent max-width, not full-width columns.

**Authentication:**
8. Click the login link to show the Authentication form. Both username and password fields should have terminal styling (underline border, dark background, orange focus color).

**Report any issues you see** (e.g., "Ranking rows have wrong color", "Comment box looks broken at 375px", "Matches page is still full-width").
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
User approves visual check across all pages at both viewport sizes.
</verification>

<success_criteria>
- User confirms terminal input styling on Home and Auth pages
- User confirms Results pages are width-constrained on desktop
- User confirms ranking/topscorer rows have interactive hover styling
- No visual regressions reported on existing Form pages or Home feed
</success_criteria>

<output>
After completion, create `.planning/phases/04-make-the-ui-more-consistent-across-all-pages/04-04-SUMMARY.md`
</output>
