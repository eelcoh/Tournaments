---
phase: 15-group-matches-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Form/Dashboard.elm
autonomous: false
requirements:
  - GROUPS-01
  - GROUPS-02
  - GROUPS-03

must_haves:
  truths:
    - "The scroll wheel shows exactly 36 matches (3 per group across 12 groups)"
    - "Group completion triggers after filling 3 matches, not 6"
    - "The dashboard description reads '36 wedstrijden in 12 groepen'"
    - "The build compiles without errors"
  artifacts:
    - path: "src/Form/Dashboard.elm"
      provides: "Updated group stage description text (36 wedstrijden)"
      contains: "36 wedstrijden in 12 groepen"
    - path: "src/Bets/Init/WorldCup2026/Tournament.elm"
      provides: "36-match filter via selectedMatches list (already implemented)"
      contains: "selectedMatches"
  key_links:
    - from: "src/Bets/Init/WorldCup2026/Tournament.elm"
      to: "answers.matches"
      via: "Bets.Init uses Tournament.matches"
      pattern: "List.filter.*selectedMatches"
    - from: "answers.matches"
      to: "scroll wheel List MatchID"
      via: "Bets.Init.Lib.groupFirstMatches + GroupMatches state"
      pattern: "groupsAndFirstMatch"
---

<objective>
Activate the 36-match group stage in the WC2026 betting form by updating the dashboard description text to reflect the reduced match count. The data filter already exists.

Purpose: The group stage filter to 36 matches already exists in Tournament.elm via `selectedMatches` (3 per group × 12 groups). The only remaining change is updating the one hardcoded count reference in the UI.

Output: Build passes with 36 matches in the scroll wheel; dashboard card reads "36 wedstrijden in 12 groepen"; group completion triggers at 3/3 matches.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-group-matches-reduction/15-CONTEXT.md

<interfaces>
<!-- Key facts extracted from codebase — no exploration needed. -->

From src/Bets/Init/WorldCup2026/Tournament.elm:
- `selectedMatches : List String` — already exists with exactly 36 IDs (3 per group × 12 groups)
- `matches : List Bets.Types.Match` — filters `allMatches` using `List.filter (selectMatch onMatchId) allMatches`
- `allMatches` contains all 72 group matches; `matches` returns only the 36 selected

The 36 selected match IDs per group:
- A: m01, m28, m53
- B: m03, m26, m51
- C: m07, m30, m49
- D: m04, m32, m60
- E: m10, m33, m55
- F: m11, m35, m58
- G: m16, m40, m64
- H: m14, m37, m66
- I: m17, m41, m61
- J: m19, m44, m70
- K: m24, m47, m71
- L: m21, m45, m68

From src/Form/Dashboard.elm line 245:
- `sectionCard groupIdx "Groepsfase" "48 wedstrijden in 12 groepen" groupProgress groupsComplete`
- This is the only hardcoded match count string — change "48" to "36"

From src/Bets/Types/Answer/GroupMatches.elm:
- `isComplete : AnswerGroupMatches -> Bool` — iterates all matches via `List.all GroupMatch.isComplete`
- `isCompleteGroup : Group -> AnswerGroupMatches -> Bool` — filters by group then calls `isComplete`
- Both automatically adapt to the reduced match set — no changes needed

From src/Bets/Init/Lib.elm:
- `groupFirstMatches` computes first match ID per group from `answers.matches` — auto-adapts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard description text for 36-match group stage</name>
  <files>src/Form/Dashboard.elm</files>
  <action>
In `src/Form/Dashboard.elm` line 245, change the static description string from `"48 wedstrijden in 12 groepen"` to `"36 wedstrijden in 12 groepen"`.

This is the only hardcoded match count in the codebase. The data layer (Tournament.elm `selectedMatches`) already filters to 36 matches. Completion tracking (`isComplete`, `isCompleteGroup`) iterates over `answers.matches` and auto-adapts. No other files need changes.

Do NOT touch Tournament.elm — the 36-match filter is already correct.
Do NOT add matchday labels or visual restyling — explicitly deferred per CONTEXT.md.
  </action>
  <verify>
    <automated>make build 2>&1 | grep -E "Success|error"</automated>
  </verify>
  <done>Build succeeds. `src/Form/Dashboard.elm` contains "36 wedstrijden in 12 groepen". No other files modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 36-match group stage in running app</name>
  <files></files>
  <action>Human verification of the running application.</action>
  <what-built>
    36-match group stage: Tournament.elm already filters to 36 matches (3 per group × 12 groups); dashboard description updated to "36 wedstrijden in 12 groepen".
  </what-built>
  <how-to-verify>
    1. Run `make build` then `python3 -m http.server --directory build` (port 8000)
    2. Open http://localhost:8000 in browser
    3. Navigate to the form (`#formulier`)
    4. Dashboard card: confirm description reads "36 wedstrijden in 12 groepen" (not 48)
    5. Open the Groepsfase section: scroll through Group A — there should be exactly 3 matches
    6. Fill in all 3 Group A matches — confirm Group A nav letter shows as complete
    7. Confirm the full scroll sequence has 36 matches across A-L (not 72)
  </how-to-verify>
  <verify>
    <automated>make build 2>&1 | grep "Success"</automated>
  </verify>
  <done>User confirms: dashboard shows "36 wedstrijden", scroll wheel shows 3 matches per group, group completion triggers at 3 filled.</done>
  <resume-signal>Type "approved" if correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `make build` compiles without errors
- `src/Form/Dashboard.elm` contains "36 wedstrijden in 12 groepen"
- Scroll wheel contains 36 total match entries across 12 groups (3 per group)
- Group completion triggers at 3/3 filled matches
</verification>

<success_criteria>
- Build passes
- Dashboard shows "36 wedstrijden in 12 groepen" description
- Scroll wheel shows 3 matches per group (36 total)
- Group A-L nav letters go green after filling 3 matches per group
- GROUPS-01, GROUPS-02, GROUPS-03 addressed
</success_criteria>

<output>
After completion, create `.planning/phases/15-group-matches-reduction/15-01-SUMMARY.md` following the summary template.
</output>
