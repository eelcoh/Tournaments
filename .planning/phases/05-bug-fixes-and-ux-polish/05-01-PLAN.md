---
phase: 05-bug-fixes-and-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Form/Bracket/View.elm
autonomous: true
requirements: [ISSUE-1, ISSUE-3]

must_haves:
  truths:
    - "Flag images appear in the computer-layout bracket grid alongside team codes"
    - "Completed rounds in the ASCII stepper show a checkmark instead of 'x'"
    - "When R32 is complete, a sticky 'Ga verder' button appears pinned to the bottom of the bracket page"
    - "The sticky button does not appear on other pages (group matches, topscorer)"
  artifacts:
    - path: "src/Form/Bracket/View.elm"
      provides: "Fixed viewTeamBadge with flag images; sticky completion button via inFront; checkmark stepper"
      contains: "flagImg"
  key_links:
    - from: "src/Form/Bracket/View.elm"
      to: "T.flagUrl"
      via: "Element.image in viewTeamBadge"
      pattern: "flagUrl.*Just team"
    - from: "src/Form/Bracket/View.elm"
      to: "Element.inFront"
      via: "outer el wrapper on page call"
      pattern: "Element.inFront"
---

<objective>
Fix two bracket wizard regressions in src/Form/Bracket/View.elm: (1) add flag images to the computer-layout team badge cells, and (2) make the "Ga verder" completion button sticky at the bottom and add checkmark indicators to completed stepper rounds.

Purpose: Issue #1 (flags regression) and Issue #3 (completion feedback + sticky button) both touch the same file. Combining them avoids two separate edits to Form/Bracket/View.elm.
Output: Updated Form/Bracket/View.elm with flag images in viewTeamBadge, checkmarks in stepper, and sticky inFront completion button.
</objective>

<execution_context>
@/home/eelco/.claude/get-shit-done/workflows/execute-plan.md
@/home/eelco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-bug-fixes-and-ux-polish/05-RESEARCH.md
@src/Form/Bracket/View.elm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flag images to viewTeamBadge (computer layout)</name>
  <files>src/Form/Bracket/View.elm</files>
  <action>
In `viewTeamBadge` (lines 525–557), add a `flagImg` let-binding that renders `Element.image [ Element.height (Element.px 16), Element.width (Element.px 16) ] { src = T.flagUrl (Just team), description = T.display team }`. Then wrap both branches (clickable and grey) so that the inner `Element.el` becomes an `Element.row [ spacing 4, Element.centerX, Element.centerY ] [ flagImg, Element.el [...] (Element.text (T.display team)) ]`. This mirrors the flag rendering in `viewSelectableTeam` (lines 391–452) and `viewPlacedBadge` (lines 560–587).

The `canSelectTeam` branch (tappable) should use `Color.primaryText`; the non-selectable branch uses `Color.grey`.

Do NOT change `viewSelectableTeam` — it already has correct flag rendering.
  </action>
  <verify>
    <automated>make build 2>&1 | tail -20</automated>
    <manual>In the bracket wizard computer layout (>500px viewport), open a round section and confirm team code cells now show a small flag icon to the left of the 3-char team code.</manual>
  </verify>
  <done>viewTeamBadge renders flag + code in both selectable and non-selectable states; make build succeeds with no Elm errors.</done>
</task>

<task type="auto">
  <name>Task 2: Checkmark in ASCII stepper + sticky "Ga verder" button</name>
  <files>src/Form/Bracket/View.elm</files>
  <action>
**A. Checkmark in steppers:**

In `viewRoundStepperFull`, in the `dotChar` function change `"x"` to `"✓"` (U+2713, Elm string literal `"\u{2713}"`):
```elm
dotChar r =
    if isComplete r then
        "\u{2713}"
    else
        "."
```

In `viewRoundStepperCompact`, in the `prefix` function change `"[x] "` to `"[✓] "`:
```elm
prefix r =
    if r == activeRound then
        "> "
    else if isComplete r then
        "[\u{2713}] "
    else
        "[ ] "
```

**B. Sticky "Ga verder" button:**

In `viewCompletionButton`, update to return a sticky overlay element when complete. Change the call site in `view` so the completion button is no longer in the page content list — instead, wrap the entire `page "bracket" ...` call in an outer `Element.el` with `Element.inFront`:

```elm
-- In view function, update the let block and return value:
let
    stickyButton =
        if isWizardComplete sel then
            Element.el
                [ Element.alignBottom
                , Element.width Element.fill
                , Element.padding 16
                ]
                (UI.Button.pill UI.Style.Focus GoNext "Ga verder \u{2192}")
        else
            Element.none
in
Element.el
    [ Element.inFront stickyButton
    , Element.width Element.fill
    ]
    (page "bracket"
        ([ stepper ] ++ sections ++ [ extroduction ])
    )
```

Remove `completionButton` from the `let` binding and from the page content list (no longer needed as a page element; it is now the `stickyButton` overlay).

Keep `viewCompletionButton` function in the file but it can be left as-is or removed — it is no longer called. Removing it avoids a dead-code warning but if the Elm compiler does not warn on unused private functions you can leave it. Best to remove it to keep the file clean.

Do NOT add `inFront` at the `Element.layout` level in View.elm — that would make the button appear on all pages. The `inFront` must be on the `Element.el` wrapper inside `Form.Bracket.View.view` only.
  </action>
  <verify>
    <automated>make build 2>&1 | tail -20</automated>
    <manual>Open the bracket wizard, fill all R32 slots. The "Ga verder →" button should appear pinned to the bottom of the viewport. Completed stepper rounds should show ✓ instead of x. Navigate to group matches or topscorer — the sticky button should NOT appear there.</manual>
  </verify>
  <done>Stepper shows ✓ for complete rounds. Sticky "Ga verder" button appears at bottom of bracket page when R32 is complete, and is absent on all other pages. make build succeeds.</done>
</task>

</tasks>

<verification>
Run `make build` — zero Elm compiler errors required.
Visually confirm in browser (or describe to checker): computer-layout group rows show flag + code in each team cell; full and compact steppers show ✓ for completed rounds; bracket page shows sticky button at bottom when R32 is done; other pages do not show the button.
</verification>

<success_criteria>
- make build produces build/main.js with no Elm errors
- viewTeamBadge (computer layout) renders flag images matching viewSelectableTeam pattern
- Completed rounds in both stepper variants use ✓ character
- "Ga verder" button is sticky (inFront + alignBottom) on bracket page only when isWizardComplete
</success_criteria>

<output>
After completion, create `.planning/phases/05-bug-fixes-and-ux-polish/05-01-SUMMARY.md` following the summary template.
</output>
